<div class="product__details__breadcrumb mb-0 bg-transparent py-4">
    <a href="{{ route('home') }}">Home</a>
    <a href="{{ route('products') }}">Products</a>
    <span>{{ $detail->name }}</span>
</div>